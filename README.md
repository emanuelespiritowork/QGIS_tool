# QGIS_tool
QGIS algorithm models

---
pulizia_vettore.model3 : algorithms that takes in input a vector, deletes duplicated vertices, finds duplicates and keeps only one, deletes polygons that overlay, fixes wrong geometries and take only GEOS-validated geometries  
